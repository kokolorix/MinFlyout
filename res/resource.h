/**
 * \file resource.h
 * \ingroup app
 * \brief Resource IDs (also read by the resource compiler).
 */
#pragma once

/// Application icon; first resource, so that Explorer uses it as the file icon.
#define IDI_APPICON 101
